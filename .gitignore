# Скомпилированные бинарники инструментов (собираются через `make tools`)
/tools/bin/

# Сборки сервисов
/bin/
*.exe

# Node
node_modules/
web/dist/
# Инкрементальная сборка TypeScript
*.tsbuildinfo

# Локальные окружения
.env
*.local

# IDE
.idea/
.vscode/

IDP.code-workspace

.playwright-mcp
# Тест-сертификаты E2E (генерируются `make e2e-certs`, не коммитятся).
deploy/tls/

# Вендоренные Helm-зависимости (idp-lib собирается `helm dependency build` из
# Chart.lock — он коммитится, а tgz-артефакт нет).
deploy/helm/idp/charts/
