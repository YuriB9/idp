## 1. Подготовка ветки

- [x] 1.1 Создать ветку `change/portal-iam-users-permissions-ux` от `master`
  (прямые коммиты в `master` запрещены)
- [x] 1.2 Убедиться, что базовый прогон зелёный: `cd web && npm ci` (при
  необходимости), `npx tsc --noEmit`, `npx vitest run`

## 2. Страница «Права»: подсказки действий/ресурсов в модалке создания

- [x] 2.1 В `web/src/pages/iam/PermissionsPage.tsx` вычислить мемоизированно
  уникальные отсортированные `action` и `resource` из `permissions`
  (`usePermissionsQuery`, тот же кэш — без новых запросов)
- [x] 2.2 Привязать поля `action`/`resource` (примитив `Input`) к нативным
  `<datalist>` (`list=...`), наполнив их уникальными значениями; сохранить
  свободный ввод НОВЫХ значений; схему `zod` и `react-hook-form` НЕ менять
- [x] 2.3 Комментарии — на русском; не вводить combobox-киты и сторонние зависимости
- [x] 2.4 Тесты `web/src/pages/iam/PermissionsPage.test.tsx`: показ доступных
  подсказок из каталога; создание права ВЫБОРОМ подсказки; создание права НОВЫМ
  значением, которого нет в подсказках; сохранить существующую проверку вызова
  `createPermission({action, resource})`
- [x] 2.5 `npx tsc --noEmit` и `npx vitest run` — зелёные

## 3. Страница «Права»: клиентский фильтр каталога

- [x] 3.1 Добавить локальный тулбар над `DataTable`: поле поиска (`Input`) по
  подстроке `action`/`resource` + фильтр по типу (`все`/`системные`/
  `пользовательские`); состояние — локальный `useState`
- [x] 3.2 Вычислять отфильтрованный массив мемоизированно и передавать в
  `DataTable` как `rows`; `DataTable` НЕ изменять (сортировка/плотность/пагинация
  работают поверх переданных строк)
- [x] 3.3 Динамический `emptyMessage`: «Ничего не найдено» при активном фильтре vs
  «Каталог прав пуст» без фильтра; доступность с клавиатуры, корректные label/aria
- [x] 3.4 Тесты: поиск по подстроке сужает таблицу; фильтр по типу `system`/`user`;
  пустой результат показывает empty-state; сортировка/плотность сохраняются при
  активном фильтре
- [x] 3.5 `npx tsc --noEmit` и `npx vitest run` — зелёные

## 4. Страница «Пользователи»: назначение роли в модалке

- [x] 4.1 Добавить кнопку «Назначить роль» в `PageHeader.actions`, открывающую
  `Dialog`; на странице вне модалки оставить только `DataTable` субъектов (+ confirm
  снятия), убрав карточку-форму над таблицей
- [x] 4.2 Перенести текущий JSX формы назначения (пикер с debounce, поиск
  `username`/`email`, выбор роли, скрытое поле канонического `subject`) в `Dialog`;
  submit — в `Dialog.footer` через `form="assign-role-form"`
- [x] 4.3 Единый сброс формы и пикера (`reset`, `setPicked(null)`,
  `setSubjectSearch("")`) при успехе (`onSuccess`) И при закрытии модалки; закрытие
  закрывает `Dialog`; возврат фокуса обеспечивает примитив `Dialog`
- [x] 4.4 Сохранить поведение прав/ошибок: 403 admin → `IamGuard` (страница); 403
  `iam:directory` → пикер скрыт с отказом; 503 → индикация, поле поиска остаётся;
  тосты; `zod .parse`; идемпотентность и инвалидация списка субъектов
- [x] 4.5 Сохранить снятие роли (`ConfirmDialog` + `useRevokeRole`), пометку
  осиротевших (`found=false`), курсорную пагинацию субъектов
- [x] 4.6 Обновить `web/src/pages/iam/UsersPage.test.tsx` под модалку: открытие/
  закрытие; пикер с debounce и назначение по каноническому `sub`; сброс при
  закрытии; валидация (нет выбранного пользователя); 403/503 справочника; снятие
  роли; пагинация — эквивалентное прежнему покрытие
- [x] 4.7 `npx tsc --noEmit` и `npx vitest run` — зелёные

## 5. Базовая a11y и финальная проверка

- [x] 5.1 Проверить роли/aria/фокус: модалка назначения и тулбар фильтра доступны с
  клавиатуры; статусы не только цветом; контраст в light/dark не нарушен
- [x] 5.2 Полный прогон `web`: `npx tsc --noEmit` + `npx vitest run` — зелёные
- [x] 5.3 Сверить, что контракт и кодоген НЕ изменены: `openapi/openapi.yaml`,
  `web/src/api/*`, `web/public/openapi.yaml` без правок; `npm run gen:check` зелёный
- [x] 5.4 Открыть PR с зелёным CI; после merge — `/opsx:archive` отдельным PR
  sync+archive
