## 1. Подготовка

- [x] 1.1 Сверить scope с docs/IDP_MVP_План.md (Этап 3, портал) и ADR-0009/0014/
  0015/0016; зафиксировать, что контракт периметра и кодоген НЕ меняются
- [x] 1.2 Создать git-ветку `change/portal-ui-design-system` от master (прямые
  коммиты в master запрещены)
- [x] 1.3 Зафиксировать базовую линию: `npm ci`, `npm run gen:check`,
  `tsc --noEmit`, `npm test` — всё зелёное до правок

## 2. Дизайн-токены и семантика статусов/критичности

- [x] 2.1 Расширить токены в `web/src/index.css`: семантическая палитра состояний и
  уровней критичности поверх `:root`/`.dark` (oklch), не ломая `ThemeProvider`/
  дефолт темы; пробросить через `@theme inline`
- [x] 2.2 Проверить контраст новых токенов на WCAG AA в обеих темах; задокументировать
  токены и правила (комментарии на русском / README слоя)
- [x] 2.3 Расширить `StatusBadge` и добавить бейдж критичности: единый источник
  правды маппинга, кодирование «цвет + иконка + текст», деградация неизвестного
  статуса в нейтральный бейдж
- [x] 2.4 vitest: бейдж кодирует значение тремя признаками; неизвестный статус
  деградирует безопасно

## 3. Единый маппинг кодов периметра

- [x] 3.1 Вынести в `web/src/lib` общий модуль `httpStatusOf` + маппинг
  `400/403/404/409/422/503 → стабильное сообщение` (без сырых внутренних ошибок)
- [x] 3.2 vitest: маппинг каждого кода в понятное сообщение; 503 — как retryable

## 4. Примитив DataTable

- [x] 4.1 Реализовать DataTable: семантический `<table>` (`role`, заголовки,
  `aria-sort`), выравнивание колонок, регулируемая плотность, опциональная «липкая»
  шапка
- [x] 4.2 Состояния loading (скелет) / empty (единый) / error (единый, без сырых
  деталей)
- [x] 4.3 Курсорная пагинация ADR-0009: проброс непрозрачного курсора
  (`next_page_token`/`next_cursor`) без интерпретации; пустой курсор → конец,
  управление скрыто; интеграция с `useInfiniteQuery`
- [x] 4.4 Клиентская сортировка для полностью загруженных малых наборов с отражением
  в `aria-sort`; доступность с клавиатуры
- [x] 4.5 vitest: loading/empty/error; курсор (следующая страница и пустой курсор);
  сортировка и `aria-sort`

## 5. Примитив Sidebar (раздвигающееся меню)

- [x] 5.1 Реализовать collapsible Sidebar: группировка разделов, подсветка активного
  маршрута (`aria-current`), icon-only режим с тултипами, off-canvas на узких экранах
- [x] 5.2 Сохранение состояния в localStorage (`idp-sidebar`) с валидацией по белому
  списку и деградацией при недоступности; ARIA-роли навигации и клавиатура
- [x] 5.3 vitest: свернуть/развернуть + сохранение/восстановление; активный пункт;
  клавиатурная навигация; недоступность localStorage не ломает меню

## 6. Примитивы Dialog / ConfirmDialog

- [x] 6.1 Реализовать Dialog: focus-trap, возврат фокуса на инициатор, ESC и клик
  overlay, блокировка скролла фона, ARIA (`role="dialog"`, `aria-modal`,
  `aria-labelledby`)
- [x] 6.2 Реализовать ConfirmDialog для деструктивных операций (наследует a11y
  Dialog); усиленное подтверждение для необратимых (ввод имени сервиса, предусловия)
- [x] 6.3 Формы в модалках — react-hook-form + zod; ответы — zod `.parse`
- [x] 6.4 vitest: открытие захватывает фокус; ESC/overlay закрывают и возвращают
  фокус; сабмит формы и блокировка невалидного ввода; confirm требует подтверждения

## 7. Примитив Toast и PageHeader

- [x] 7.1 Реализовать Toast: успех/ошибка, единый маппинг кодов периметра (модуль из
  §3), `role="status"`/`aria-live`, уважение `prefers-reduced-motion`
- [x] 7.2 Реализовать `PageHeader` (заголовок + действия) для консистентности страниц
- [x] 7.3 vitest: тост успеха объявляется; маппинг кода ошибки → сообщение; 503 —
  деградация

## 8. Рефактор каркаса и каталога сервисов

- [x] 8.1 Перевести `GlobalLayout` на Sidebar + PageHeader, сохранив маршруты,
  `ThemeToggle` и ссылку Swagger
- [x] 8.2 Перевести `ServiceListPage` на DataTable (курсор `next_page_token`,
  состояния, сортировка), бейджи статусов/критичность; переход к детали сохранить
- [x] 8.3 vitest: каталог на DataTable сохраняет поведение (отображение, переход,
  zod `.parse`, состояния)

## 9. Рефактор детали сервиса и действий

- [x] 9.1 Перевести `ServiceProgressPage` на PageHeader + StatusBadge, сохранив
  поллинг до терминального статуса
- [x] 9.2 Перенести `DecommissionCard`/`TransferCard`/`OwnersCard` на
  Dialog/ConfirmDialog: сохранить ввод имени, предусловия (`load_drained`),
  optimistic-concurrency владельцев; результат — через Toast (маппинг 403/409/422)
- [x] 9.3 vitest: действия через модалку/confirm сохраняют гарантии; тосты успех/ошибка

## 10. Рефактор создания сервиса

- [x] 10.1 Перевести `CreateServicePage` на единый паттерн формы (react-hook-form +
  zod) в Dialog/на странице; конфликт имени (409) — через Toast
- [x] 10.2 vitest: валидация имени блокирует отправку; 409 показывается тостом без
  сырых деталей

## 11. Рефактор раздела «Роли и доступы»

- [x] 11.1 Перевести таблицы ролей/прав/субъектов на DataTable (курсор
  `next_page_token`), сохранив системные роли/права как read-only
- [x] 11.2 Перенести структурные действия (create/delete роли и права, attach/detach)
  на Dialog/ConfirmDialog; результаты — через Toast (маппинг 400/403/404/409/422)
- [x] 11.3 Сохранить пикер пользователя из справочника (debounce, `username`/`email`,
  канонический `sub`, `next_cursor`) и индикацию недоступности каталога
- [x] 11.4 Сохранить обработку: 403 на справочник скрывает пикер; 503 оставляет поиск;
  403 на админку — fail-closed (нет содержимого); «осиротевшие» (`found=false`)
  помечены
- [x] 11.5 vitest: пикер сохранён; 403 справочник/админка; 503; осиротевший субъект;
  структурные действия в модалках

## 12. Доступность (сквозная проверка)

- [x] 12.1 Проверить клавиатурную управляемость меню/таблиц/диалогов/форм и видимый
  фокус; ARIA-роли (покрыто тестами Sidebar/Dialog/DataTable + сводный a11y.test)
- [x] 12.2 Проверить контраст WCAG AA в обеих темах (включая бейджи статусов):
  семантические токены подобраны под AA, оформление статусов/критичности — не только
  цветом (цвет+иконка+текст)
- [x] 12.3 Проверить `prefers-reduced-motion` для тостов/модалок/анимаций меню
  (переходы только в `motion-safe`; проверено a11y-тестом)
- [x] 12.4 vitest: базовые a11y-проверки (роли/aria, управление фокусом)

## 13. Завершение

- [x] 13.1 Прогнать `npm run gen:check` (без diff), `tsc --noEmit`, `npm test` —
  зелёные (плюс `npm run build` — успешен)
- [x] 13.2 Убедиться, что `web/src/api/*`, `web/public/openapi.yaml`,
  `openapi/openapi.yaml` НЕ изменены (отсутствуют в diff)
- [ ] 13.3 Открыть PR; дождаться полного зелёного CI (go test всех модулей,
  golangci-lint, govulncheck, gen:check, openapi-lint, web-test, integration,
  conformance)
