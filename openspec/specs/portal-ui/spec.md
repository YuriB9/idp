# portal-ui Specification

## Purpose
TBD - created by syncing change create-service-portal. Update Purpose after archive.
## Requirements
### Requirement: SPA-портал поднимается как реальное приложение

Модуль `./web` ДОЛЖЕН (MUST) быть полноценным SPA на React 19 + Vite +
TypeScript с роутингом (react-router) и `QueryClient` (TanStack Query):
`index.html`, `main.tsx`, корневой `App`. ДОЛЖНЫ (MUST) присутствовать
npm-скрипты `dev`/`build`/`preview` и dev-прокси `/api` → gateway. OpenAPI
остаётся единственным источником правды для TS-клиента/zod (кодоген `gen` без
diff в CI).

#### Scenario: Сборка и запуск

- **WHEN** выполняется `npm run build`
- **THEN** Vite собирает SPA без ошибок типизации

#### Scenario: Dev-прокси периметра

- **GIVEN** запущенный gateway
- **WHEN** в dev-режиме (`npm run dev`) портал шлёт запрос на `/api/...`
- **THEN** dev-сервер Vite проксирует его на gateway, без CORS-ошибок в браузере

### Requirement: Экран списка сервисов проекта

Портал ДОЛЖЕН (MUST) показывать экран со списком сервисов проекта, получая
данные через `GET /api/projects/{project}/services`. Ответ ДОЛЖЕН (MUST)
валидироваться рантайм через сгенерированную из OpenAPI zod-схему (`.parse`);
дрейф контракта ДОЛЖЕН (MUST) падать явно.

#### Scenario: Отображение списка

- **GIVEN** периметр вернул валидный список сервисов
- **WHEN** открывается экран списка
- **THEN** сервисы отображаются с именем и статусом

#### Scenario: Дрейф контракта падает явно

- **GIVEN** ответ API не соответствует zod-схеме
- **WHEN** портал парсит ответ через `.parse`
- **THEN** выбрасывается ошибка валидации (несоответствие не маскируется тихо)

### Requirement: Форма создания сервиса

Портал ДОЛЖЕН (MUST) предоставлять форму создания сервиса на react-hook-form,
где источником правды валидации входных данных служит zod-схема из кодогена.
Отправка формы ДОЛЖНА (MUST) вызывать `POST /api/projects/{project}/services` и
по успеху переводить пользователя на экран прогресса создаваемого сервиса.

#### Scenario: Успешная отправка

- **GIVEN** заполнено валидное имя сервиса
- **WHEN** пользователь отправляет форму
- **THEN** портал шлёт `POST`, получает `id`/`status:"creating"` и переходит на
  экран прогресса

#### Scenario: Клиентская валидация имени

- **GIVEN** имя пустое или не проходит zod-схему
- **WHEN** пользователь пытается отправить форму
- **THEN** форма показывает ошибку валидации и НЕ шлёт запрос

#### Scenario: Конфликт имени с сервера

- **GIVEN** периметр вернул 409 (имя занято)
- **WHEN** обрабатывается ответ
- **THEN** форма показывает понятное сообщение о конфликте без раскрытия
  внутренних деталей сервера

### Requirement: Экран прогресса с поллингом статуса

Портал ДОЛЖЕН (MUST) иметь экран/индикатор прогресса создания, который ПОЛЛИТ
`GET /api/projects/{project}/services/{name}` (через TanStack Query с
интервалом) до достижения терминального статуса `active` или `failed`, после
чего опрос ДОЛЖЕН (MUST) останавливаться и показываться исход. Все ответы
поллинга ДОЛЖНЫ (MUST) валидироваться через zod `.parse`.

#### Scenario: Поллинг до ACTIVE

- **GIVEN** сервис в статусе `creating`
- **WHEN** экран прогресса периодически опрашивает `GetService`
- **THEN** при переходе статуса в `active` опрос останавливается и показывается
  успешный исход

#### Scenario: Поллинг до FAILED

- **GIVEN** сервис в статусе `creating`
- **WHEN** провизия завершилась откатом (Saga) и статус стал `failed`
- **THEN** опрос останавливается и показывается исход с ошибкой создания

#### Scenario: Промежуточный статус не завершает опрос

- **GIVEN** очередной ответ снова `creating`
- **WHEN** обрабатывается ответ поллинга
- **THEN** опрос продолжается с заданным интервалом (терминал не достигнут)

### Requirement: Отображение и изменение владельцев в портале

Портал ДОЛЖЕН (MUST) отображать владельцев сервиса на экранах чтения (список/
карточка сервиса) и предоставлять форму изменения состава владельцев
(react-hook-form + zod как источник правды формы, мутации через TanStack Query).
Форма ДОЛЖНА (MUST) отправлять полный желаемый набор владельцев и текущую версию
`owners_version` (optimistic-concurrency). Ответы API ДОЛЖНЫ (MUST) валидироваться
через zod `.parse`. UI ДОЛЖЕН (MUST) обрабатывать `403` (нет права) и `409`
(конфликт версии — устаревшие данные) понятным сообщением и НЕ ДОЛЖЕН (MUST NOT)
показывать сырые внутренние ошибки. Все комментарии в коде — на русском языке.

#### Scenario: Просмотр владельцев

- **GIVEN** сервис с владельцами `{alice, bob}`
- **WHEN** пользователь открывает карточку/список сервиса
- **THEN** владельцы отображаются; данные ответа проходят zod `.parse`

#### Scenario: Успешное изменение владельцев

- **GIVEN** форма изменения владельцев с валидным набором и текущей версией
- **WHEN** пользователь сохраняет изменения
- **THEN** выполняется мутация `PUT .../owners`, после успеха список владельцев и
  кэш TanStack Query обновляются

#### Scenario: Отказ доступа (403)

- **GIVEN** у пользователя нет права `change_owners`
- **WHEN** он отправляет форму изменения владельцев
- **THEN** UI показывает понятное сообщение об отказе доступа, без раскрытия
  внутренних деталей

#### Scenario: Конфликт версии (409)

- **GIVEN** владельцы были изменены конкурентно (версия устарела)
- **WHEN** пользователь сохраняет форму со старой версией
- **THEN** UI сообщает о конфликте/устаревших данных и предлагает обновить
  состояние перед повтором

### Requirement: Действие «Вывести из эксплуатации» с подтверждением

Портал ДОЛЖЕН (MUST) предоставлять действие «Вывести из эксплуатации» на странице
сервиса (по образцу карточки владельцев/формы создания: react-hook-form + zod,
TanStack Query). Действие ДОЛЖНО (MUST) требовать явного подтверждения с вводом
имени сервиса и явно сообщать, что это decommission (soft-delete, необратимость для
доступа), а не purge. Действие ДОЛЖНО (MUST) отправлять явное предусловие снятой
нагрузки (`load_drained`). Кнопка/действие ДОЛЖНЫ (MUST) быть недоступны или
заблокированы для сервиса, который уже `decommissioned` или находится в
недопустимом статусе. Ответы API ДОЛЖНЫ (MUST) валидироваться через zod (`.parse`).

#### Scenario: Подтверждение и успешный вывод

- **GIVEN** активный сервис на странице портала
- **WHEN** пользователь выбирает «Вывести из эксплуатации», вводит имя сервиса для
  подтверждения и подтверждает (со снятой нагрузкой)
- **THEN** отправляется `POST .../decommission`, по успеху статус в UI отражается
  как `decommissioned`

#### Scenario: Блокировка повторного/недопустимого действия

- **GIVEN** сервис уже в статусе `decommissioned` (или `creating`/`failed`)
- **WHEN** отображается страница сервиса
- **THEN** действие «Вывести из эксплуатации» недоступно/заблокировано

### Requirement: Обработка ошибок и индикация статуса в портале

Портал ДОЛЖЕН (MUST) корректно обрабатывать ответы операции вывода из эксплуатации:
`403` (нет права) — сообщение об отказе доступа; `409` (конкурентный конфликт) —
сообщение о конфликте с предложением перечитать состояние; `422` (предусловие не
выполнено, нагрузка не снята) — понятное сообщение о невыполненном предусловии.
Статус `decommissioned` ДОЛЖЕН (MUST) индицироваться бейджем статуса в списке и на
странице сервиса; `decommissioned_at` ДОЛЖЕН (MUST) отображаться для выведенных
сервисов.

#### Scenario: Отказ доступа (403)

- **GIVEN** у пользователя нет права `decommission`
- **WHEN** он пытается вывести сервис из эксплуатации
- **THEN** UI показывает сообщение об отказе доступа, статус не меняется

#### Scenario: Предусловие не выполнено (422)

- **GIVEN** нагрузка не снята
- **WHEN** API возвращает `422`
- **THEN** UI показывает понятное сообщение о невыполненном предусловии (нагрузка
  не снята)

#### Scenario: Конфликт (409)

- **GIVEN** статус сервиса изменился конкурентно
- **WHEN** API возвращает `409`
- **THEN** UI показывает сообщение о конфликте и предлагает перечитать состояние

### Requirement: Действие «Перенести сервис» с подтверждением и предупреждением о рисках

Портал ДОЛЖЕН (MUST) предоставлять действие «Перенести сервис» на странице сервиса
(по образцу `DecommissionCard`/`OwnersCard`: react-hook-form + zod, TanStack Query).
Действие ДОЛЖНО (MUST) требовать явного подтверждения с вводом имени сервиса и явно
предупреждать о НЕОБРАТИМОСТИ/рисках (transfer репозитория GitLab и миграция путей
Vault частично необратимы — точка невозврата). Действие ДОЛЖНО (MUST) позволять
выбрать/указать target-проект. Кнопка/действие ДОЛЖНЫ (MUST) быть недоступны или
заблокированы для сервиса не в статусе `active` (`creating`/`failed`/
`decommissioned`/`transferring`). Ответы API ДОЛЖНЫ (MUST) валидироваться через zod
(`.parse`).

#### Scenario: Подтверждение и успешный перенос

- **GIVEN** активный сервис на странице портала
- **WHEN** пользователь выбирает «Перенести сервис», указывает target-проект,
  вводит имя сервиса для подтверждения (с подтверждением осознания рисков) и
  подтверждает
- **THEN** отправляется `POST /projects/{project}/services/{name}/transfer` с
  `{target_project}`, по успеху статус в UI отражает переход (`transferring`, затем
  итоговый проект/`active`)

#### Scenario: Блокировка для недопустимого статуса

- **GIVEN** сервис в статусе `transferring` (или `creating`/`failed`/
  `decommissioned`)
- **WHEN** отображается страница сервиса
- **THEN** действие «Перенести сервис» недоступно/заблокировано

### Requirement: Обработка ошибок и индикация статуса переноса в портале

Портал ДОЛЖЕН (MUST) корректно обрабатывать ответы операции переноса: `403` (нет
права на source или target) — сообщение об отказе доступа; `409` (занятое имя в
target или конкурентный конфликт) — сообщение о конфликте с предложением выбрать
другой проект/перечитать состояние; `422` (недопустимый исходный статус) — понятное
сообщение о невыполненном предусловии. Статус `transferring` ДОЛЖЕН (MUST)
индицироваться бейджем статуса (`StatusBadge`) в списке и на странице сервиса.

#### Scenario: Отказ доступа (403)

- **GIVEN** у пользователя нет права `transfer` на source или `transfer_in` на target
- **WHEN** он пытается перенести сервис
- **THEN** UI показывает сообщение об отказе доступа, статус не меняется

#### Scenario: Конфликт занятого имени (409)

- **GIVEN** в target-проекте уже есть сервис с тем же именем
- **WHEN** API возвращает `409`
- **THEN** UI показывает сообщение о конфликте и предлагает выбрать другой
  target-проект/перечитать состояние

#### Scenario: Индикация transferring

- **GIVEN** сервис находится в процессе переноса
- **WHEN** отображается список/страница сервиса
- **THEN** `StatusBadge` показывает статус `transferring`

### Requirement: Раздел «Роли и доступы» (IAM-админка) в портале

Портал ДОЛЖЕН (MUST) предоставлять новый горизонтальный раздел «Роли и доступы»
(новый маршрут react-router, не вложенный в project-scope) с тремя областями:
read-only таблица ролей и их прав, список субъектов с их ролями (keyset-пагинация
через `next_page_token`), и форма назначить/снять роль субъекту. Чтение и мутации
ДОЛЖНЫ (MUST) идти через сгенерированный из OpenAPI zodios-клиент с рантайм-
валидацией ответов (`zod .parse`); сырые внутренние ошибки клиенту НЕ показываются.
Назначение/снятие роли ДОЛЖНО (MUST) использовать react-hook-form + zod (валидация
ввода) и TanStack-мутацию с инвалидацией соответствующих запросов
(субъекты/роли субъекта) после успеха.

#### Scenario: Просмотр ролей, прав и субъектов

- **GIVEN** у субъекта есть право `(read, iam:global)`
- **WHEN** пользователь открывает раздел «Роли и доступы»
- **THEN** отображаются таблица ролей с их правами и список субъектов с их ролями;
  ответы проходят рантайм-валидацию zod

#### Scenario: Назначение и снятие роли с обновлением списка

- **GIVEN** пользователь с правом `(write, iam:global)` в разделе
- **WHEN** он назначает роль субъекту (форма), затем снимает её
- **THEN** выполняется соответствующая мутация, после успеха запросы субъекта/ролей
  инвалидируются и список обновляется, показывается индикация результата

#### Scenario: Валидация ввода формы

- **WHEN** пользователь отправляет форму с пустым subject или без выбранной роли
- **THEN** zod-валидация react-hook-form блокирует отправку и показывает сообщение,
  запрос не уходит

### Requirement: Обработка отказа доступа и ошибок в разделе «Роли и доступы»

Раздел «Роли и доступы» ДОЛЖЕН (MUST) корректно обрабатывать `403` (нет права на
админку — раздел заблокирован либо показан явный отказ, без аварии приложения),
`404` (несуществующая роль при назначении/просмотре прав роли) и `400` (валидация),
показывая стабильные пользовательские сообщения и НЕ раскрывая сырые внутренние
детали. При `403` пользователь НЕ ДОЛЖЕН (MUST) видеть содержимое каталога
(fail-closed на UI согласуется с fail-closed периметра).

#### Scenario: Нет права на админку → отказ без содержимого

- **GIVEN** у субъекта нет права `(read, iam:global)` (периметр отвечает `403`)
- **WHEN** пользователь открывает раздел «Роли и доступы»
- **THEN** раздел показывает отказ доступа (или заблокирован), содержимое каталога
  не отображается, сырые ошибки не раскрываются

#### Scenario: Несуществующая роль при назначении → понятная ошибка

- **GIVEN** выбрана несуществующая роль (периметр отвечает `404`)
- **WHEN** пользователь подтверждает назначение
- **THEN** показывается стабильное сообщение об ошибке, список не повреждается

### Requirement: Управление каталогом ролей и прав в разделе «Роли и доступы»

Портал ДОЛЖЕН (MUST) в разделе «Роли и доступы» предоставлять структурное
управление каталогом: создание роли (`POST /iam/roles`), удаление роли
(`DELETE /iam/roles/{role}`), правку набора прав роли (attach/detach из списка
существующих прав), создание права (`POST /iam/permissions`). Формы ДОЛЖНЫ (MUST)
использовать react-hook-form + zod (валидация ввода: непустое имя роли, выбранное
право и т.п.) и TanStack-мутации с инвалидацией соответствующих запросов
(`["iam","roles"]`, `["iam","permissions"]`, права роли) после успеха. Ответы
ДОЛЖНЫ (MUST) проходить рантайм-валидацию zod `.parse`; сырые внутренние ошибки НЕ
показываются. Удаление существующего права/роли — через тот же сгенерированный из
OpenAPI клиент.

#### Scenario: Создание и удаление роли с обновлением списка

- **GIVEN** пользователь с правом `(manage, iam:global)` в разделе
- **WHEN** он создаёт роль (форма), затем удаляет пользовательскую роль
- **THEN** выполняются соответствующие мутации, после успеха запрос ролей
  инвалидируется, список обновляется, показывается индикация результата

#### Scenario: Attach и detach права роли

- **GIVEN** выбрана пользовательская роль
- **WHEN** пользователь прикрепляет право из списка, затем открепляет его
- **THEN** выполняются attach/detach-мутации, набор прав роли обновляется из тела
  ответа (`RolePermissions`), список прав роли перерисовывается

#### Scenario: Создание права

- **WHEN** пользователь заполняет форму нового права (`action`, `resource`) и
  отправляет
- **THEN** выполняется мутация создания, список прав инвалидируется и обновляется

#### Scenario: Валидация ввода формы

- **WHEN** пользователь отправляет форму создания роли с пустым именем или attach
  без выбранного права
- **THEN** zod-валидация react-hook-form блокирует отправку и показывает сообщение,
  запрос не уходит

### Requirement: Read-only системных ролей/прав и обработка ошибок каталога

Раздел «Роли и доступы» ДОЛЖЕН (MUST) отображать СИСТЕМНЫЕ роли/права (`system=true`)
как read-only: показывать бейдж «системная», а кнопки удаления/правки набора прав
для них скрывать или блокировать (UI согласован с защитой периметра — попытка всё
равно вернёт `422`). Раздел ДОЛЖЕН (MUST) корректно обрабатывать `403` (нет права
`manage` — структурные действия заблокированы/скрыты, без аварии), `404`
(несуществующая роль/право), `409` (дубль имени роли/пары права) и `422` (попытка
изменить системную роль/право), показывая стабильные пользовательские сообщения и
НЕ раскрывая сырые внутренние детали.

#### Scenario: Системная роль показана как read-only

- **GIVEN** роль `iam-admin` имеет `system=true`
- **WHEN** пользователь открывает раздел
- **THEN** рядом с ролью показан бейдж «системная», кнопки удаления и правки прав
  скрыты/заблокированы

#### Scenario: Нет права manage → структурные действия скрыты

- **GIVEN** у субъекта есть `(read, iam:global)`, но нет `(manage, iam:global)`
  (структурные мутации отвечают `403`)
- **WHEN** пользователь открывает раздел
- **THEN** формы создания/удаления/правки скрыты или заблокированы, сырые ошибки не
  раскрываются (просмотр каталога при наличии `read` остаётся доступен)

#### Scenario: Дубль имени роли → понятная ошибка

- **GIVEN** роль с таким именем уже существует (периметр отвечает `409`)
- **WHEN** пользователь подтверждает создание
- **THEN** показывается стабильное сообщение о конфликте, список не повреждается

#### Scenario: Попытка изменить системную роль → понятная ошибка

- **GIVEN** клиент обошёл UI и отправил мутацию системной роли (периметр отвечает
  `422`)
- **WHEN** приходит ответ `422`
- **THEN** показывается стабильное сообщение о защите системной роли, без раскрытия
  внутренних деталей

